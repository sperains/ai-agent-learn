[project]
name = "ai-agent-learn"
version = "0.1.0"
description = "A reproducible learning lab for AI Agent engineering"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3",
]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["src"]
testpaths = ["tests"]
