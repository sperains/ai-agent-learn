# 记录第 8 课工具结果回传

## 已完成

- 实现两轮工具调用流程。
- 将加法结果作为 `tool` 消息回传给模型。
- 未知工具失败时不进入第二轮。
- 完整测试得到 `30 passed`。

## 当前结论

工具结果是模型下一步决策所需的 Observation。应用不能吞掉失败信息，但仍负责限制重试和工具权限。

## 下一步

进入第 9 课，手写最小 Agent Loop。
