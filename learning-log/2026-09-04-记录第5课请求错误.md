# 记录第 5 课请求错误

## 本次目标

理解模型 HTTP 请求中的超时、限流和服务端失败，并建立最小的重试分类策略。

## 已完成

- 实现超时、限流、客户端、服务端和未知错误类别。
- 根据错误类别独立判断是否值得重试。
- 覆盖超时、`429`、`400` 和 `503` 四类场景。
- 完整测试得到 `21 passed`。

## 当前结论

超时只说明应用未及时收到响应。重试策略必须按错误类别处理，限制次数并逐步延长间隔，避免在限流或故障期间放大压力。

## 下一步

进入第 6 课，学习工具调用的协议本质。
