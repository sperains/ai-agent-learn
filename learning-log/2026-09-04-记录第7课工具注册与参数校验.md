# 记录第 7 课工具注册与参数校验

## 本次目标

建立工具白名单和参数校验，避免模型错误意图直接进入工具执行。

## 已完成

- 实现加法工具的注册表。
- 拒绝未注册的工具名。
- 在执行前校验必填参数和数字类型。
- 拒绝 Python 中可被静默转为数字的布尔值。
- 完整测试得到 `28 passed`。

## 当前结论

模型只能请求已注册工具。参数校验是执行边界的一部分，错误参数不能依赖底层语言的隐式转换后继续执行。

## 下一步

进入第 8 课，学习工具结果回传。
