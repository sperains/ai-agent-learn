# 记录第 4 课结构化输出

## 本次目标

将模型文本解析为确定的任务分类结果，并阻止无效数据进入业务逻辑。

## 已完成

- 实现任务分类结果对象和 JSON 解析入口。
- 校验 JSON 顶层对象、布尔字段和允许的分类值。
- 覆盖合法输出、无效 JSON、字段类型错误和类别错误四类测试。
- 修正响应内容无效时抛出异常类型不一致的问题。
- 完整测试得到 `17 passed`。

## 当前结论

Prompt 只能约束模型输出倾向，不能取代程序校验。解析失败或结构不符合契约时，应明确失败；后续可增加有限重试、记录和降级，而不能静默默认。

## 下一步

进入第 5 课，学习 HTTP、API 与错误。
